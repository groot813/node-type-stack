{
  "name": "my-movie-manager",
  "version": "1.0.0",
  "description": "complete typescript stack",
  "main": "index.js",
  "scripts": {
    "installAllLinux": "npm install --prefix backend & npm install --prefix frontend/my-movie-manager",
    "dev": "npm run dev --prefix backend & npm start --prefix frontend/my-movie-manager",
    "run": "ts-node main.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maurice Reigman",
  "license": "pending (probably ISC)"
}
